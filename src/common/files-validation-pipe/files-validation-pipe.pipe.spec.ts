import { FilesValidationPipePipe } from './files-validation-pipe.pipe';

describe('FileValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new FilesValidationPipePipe()).toBeDefined();
  });
});
